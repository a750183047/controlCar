<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="yan.com.ctrl"
    android:versionCode="2"
    android:versionName="1.1">


    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/yaogan"
        android:theme="@style/AppTheme">

        <activity
            android:name=".bluetooth.bluetooth.BluetoothComm"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".bluetooth.bluetooth.ScanDeviceActivity"
            android:theme="@android:style/Theme.Dialog">

        </activity>
        <activity android:name=".bluetooth.activity.FirstActivity"
            >

        </activity>
    </application>

</manifest>
